<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Task Mate Theme -->
    <style name="Base.Theme.TaskMate" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/firstBlue</item>
    </style>

    <style name="Theme.TaskMate" parent="Base.Theme.TaskMate" />

    <!-- Splash Screen Theme -->
    <style name="Theme.TaskMate.MySplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/backGroundBlue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/target</item>
        <item name="postSplashScreenTheme">@style/Theme.TaskMate</item>
    </style>
    </resources>
